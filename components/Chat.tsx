<div className="flex items-center gap-2 flex-wrap">
  <select ...>...</select>
  <button onClick={newSession} className="...">New</button>
  <button onClick={renameSession} className="...">Rename</button>
  <button onClick={duplicateSession} className="...">Duplicate</button>
  <button onClick={deleteSession} className="...">Delete</button>
  <button onClick={exportSession} className="...">Export</button>
  <span ...>{stats}</span>
</div>
